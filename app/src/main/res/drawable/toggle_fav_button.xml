<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está desmarcado -->
    <item android:drawable="@android:drawable/star_off" android:state_checked="false" android:tint="@color/unchecked_icon_color"/>
    <!-- Estado cuando el botón está marcado -->
    <item android:drawable="@android:drawable/star_on" android:state_checked="true" android:tint="@color/checked_icon_color"/>
</selector>

